build/
pkg/
*.zip
*.o
*.so
*.a

.cxx/
.gradle/
local.properties

.vscode/
.idea/
*.iml

.DS_Store
Thumbs.db

jni/zygisk.hpp

# L3 LSPlant (opt-in): vendored libs + generated callback DEX header
jni/external/
jni/hook_dex.h

# Claude Code worktrees (ephemeral; never commit as embedded repos)
.claude/worktrees/

# Runtime-generated operator selection (ditulis rotate_ids.sh saat runtime;
# jangan pernah di-commit — sumber datanya adalah data/carriers.tsv)
carrier.conf

# Host-compiled unit-test binaries (source: tests/*_test.cpp; run via tools/validate.sh)
/carrier_test
/native_read_test
/ident_synth_test
